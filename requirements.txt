# Core pipeline
openai-whisper>=20240930
pysrt>=1.1.2
streamlit>=1.40

# LangChain 1.x stack (orchestration + RAG)
langchain>=1.0
langchain-core>=1.0
langchain-openai>=1.0
langchain-community>=0.3
langchain-text-splitters>=0.3
faiss-cpu>=1.8
pydantic>=2.7

# Notes:
# - ffmpeg is a SYSTEM dependency, not a pip package. Install separately:
#     Ubuntu/Debian:  sudo apt install ffmpeg
#     macOS (brew):   brew install ffmpeg
# - openai-whisper pulls in torch; first run downloads the chosen model.
# - For an offline embedding alternative, add: langchain-huggingface, sentence-transformers
# - To use Chroma instead of FAISS, add: langchain-chroma
